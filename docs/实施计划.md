# pipeline.py 缺失功能实施计划

## 环境分析

- **当前文件**: `src/core/pipeline.py` (HEAD, 601行)
- **目标**: 恢复工作代码 (约798行) 的完整功能
- **关键约束**: 保留 HEAD 已有的新功能 (`_progress_listeners` + add/remove 函数)

## 外部依赖关系

| 外部消费者 | 使用的接口 | 影响 |
|-----------|-----------|------|
| `tracking.py` | `add_progress_listener`, `remove_progress_listener` | 不能删除/改变签名 |
| `orchestrator.py` | `Pipeline(repo, client)` 构造 | 加 `position=None` 默认值安全 |
| `scheduler.py` | `Pipeline(repo, client)` 构造 + `.crawl_once()` | 同上 |
| `cli/crawling.py` | `Pipeline(repo, client)` 构造 | 同上 |
| `core/__init__.py` | `from .pipeline import Pipeline` | 不变 |

内部方法 (`_transform_episodes`, `_build_anime_sheet_rows` 等) 无外部引用，安全修改。

---

## 实施顺序（按难度 4 档递进）

### 🟢 第一档：单行/几行改动 (5项，无依赖)

**#12** 加 `from typing import Any` — `_build_anime_sheet_rows` 使用了 `dict[str, Any]` 但未导入
- 位置：第13行 `from datetime import datetime` → 上面加一行

**#1** `__init__` 加 `position` 参数  
- 参数签名加 `position: int | None = None`，体加 `self._position = position`
- 所有现有调用用默认值 None，不破坏兼容

**#3** `fetch_and_save_episode_stats` tqdm 加参数
- tqdm 调用加 `position=self._position, leave=False`

**#6** `_build_anime_sheet_rows` 加"评论"字段
- 在"弹幕"行后插入 `"评论": getattr(stat, "reply", 0) if stat else 0,`

**#10** `_transform_season_stat` 加 `reply` 字段
- `AnimeStat` 模型已有 `reply` 列，在 `share` 后插入

### 🟡 第二档：新增独立方法/函数 (2项，无依赖)

**#9** 新增 `_classify_episode_type` 静态方法
- 独立方法，不依赖任何已有改动
- 位置：在 `_transform_season_info` 之后

**#13** 新增 `_notify_progress` 模块函数
- 依赖已有的 `_progress_listeners`（HEAD 保留）
- 位置：在 `remove_progress_listener` 之后
- 注：HEAD 的 `_on_progress` 已内联通知逻辑，此函数作为可复用辅助

### 🟠 第三档：逻辑块插入 (2项)

**#4** `crawl_once` 加 `main_count==0` 检查
- 在 `[1/5] OK` 后插入跳过逻辑块
- 依赖现有的 `datetime` 导入（已有）

**#2** 重命名调用 `_transform_episodes` → `_transform_all_episodes`
- 仅改第175行的方法名，与 #11 配合

### 🔴 第四档：复杂重写 (4项，互有依赖)

依赖链: #9 → (#11, #7) → (#2 配合 #11), #8 依赖 #7

**#11** `_transform_episodes` → `_transform_all_episodes` 完整重写
- 新增 `_build_ep` 内部函数
- 增加 `episode_type` 字段 + sections 遍历
- 输出顺序：main → special → trailer → misc

**#5** 增强版分集累加逻辑
- 替换 crawl_once 中的简单累加（第297-304行）
- 全量 reply/favorite 累加 + 主集 views/danmaku 等 max 修正

**#7** `_build_ep_sheet_rows` 完整重写
- 新增 `_make_row` 内部函数
- 正片展开 + section_sums 汇总
- `_add_summary` 内部函数
- 输出：正片行 → 花絮汇总 → 预告汇总 → 杂项汇总

**#8** `_save_raw_excel` 完整重写
- 双 Sheet → 单 Sheet "动画统计"
- 动画数据 row 0, 分集数据 row `len(anime_rows)+3`
- 格式检测 + 列宽上限 min(max_w*1.5, 60)

---

## HEAD 已有新功能（不可破坏）

以下为 HEAD 版本已有的新功能，实施时必须保留：

1. `_progress_listeners: list = []` (第30行) — 模块级监听器列表
2. `add_progress_listener(callback)` (第33行) — 注册进度回调
3. `remove_progress_listener(callback)` (第37行) — 使用 try/except 安全移除
4. `fetch_and_save_episode_stats` 中的内联进度通知 (第212-220行)
5. `_on_progress` 内嵌函数使用 `nonlocal progress_count` + 直接遍历 `_progress_listeners`

## 危险标记

- ⚠️ #11 和 #2 必须同步完成，否则第175行调用不存在的 `_transform_all_episodes` 会报错
- ⚠️ #7 和 #8 是耦合的（#8 调用 #7），但可以分步实施
- ⚠️ #9 如果单独添加不会被任何代码调用（无副作用），安全
